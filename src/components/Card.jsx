import Title from "./Title";

const Card = () => {
  return (
    <div
      style={{
        backgroundColor: "#DEDEDE",
        width: "322px",
        height: "222px",
        borderRadius: "16px 16px 0 0",
      }}
    ></div>
  );
};

export default Card;
